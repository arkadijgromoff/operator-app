services:
  - type: web
    name: operator-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "10000"
      - key: PYTHON_VERSION
        value: "3.9.0"
